version: '3.7'

services:

  web:
    image: {{ image }}
    command: run-services web
    environment:
      APP_NAME: {{ project }}
      APP_ENV: local
    ports:
      - "8000:80"
    volumes:
      - .:/app
